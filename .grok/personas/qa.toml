description = "QA — runs acceptance gates in shell (mvn test, scans); never read-only"
instructions_file = ".grok/prompts/qa.md"
default_isolation = "none"
default_capability_mode = "all"
reasoning_effort = "high"